server {
        root /var/www/mfe-uni/cart

        index index.html index.htm index.nginx-debian.html;

        server_name cart.mfe-uni.germans.dev;

        location / {
                try_files $uri $uri/ /index.html;
        }
}
